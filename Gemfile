source 'https://rubygems.org'

gem 'bundler_test2', :git => 'git@github.com:magneland/bundler_test2.git' # this depends on activesupport (< 4.0, >= 2.3.5)
gem 'factory_girl' # this depends on activesupport >= 3.0.0
